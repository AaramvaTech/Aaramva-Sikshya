# compiled output
/dist
/node_modules
/build

# environment / secrets
.env*
!.env.local

# logs
logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea
.vscode

# tests
/coverage

# Prisma generated client (regenerated via 'prisma generate' — lives in node_modules)
# node_modules/.prisma/client is already covered by /node_modules above
