{
  "name": "@aaramva/database",
  "version": "1.0.0",
  "description": "Prisma schema, migrations and seed for Aaramva Shikshya (public/platform schema)",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
